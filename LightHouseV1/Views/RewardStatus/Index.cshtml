@model IEnumerable<LightHouseV1.Models.RewardStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RewardStatusType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RewardStatusType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RewardStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RewardStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RewardStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
